{
  "name": "agentic-scale-backend",
  "version": "1.0.0",
  "private": true,
  "description": "API + pipeline de ingesta para el Radar de Noticias (Track 5 - Inteligencia de Mercado)",
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "ingest:once": "node src/scripts/runIngestOnce.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init",
    "prisma:deploy": "prisma migrate deploy",
    "prisma:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0",
    "axios": "^1.7.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "prisma": "^5.20.0"
  }
}
